---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: coredns
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  url: oci://ghcr.io/coredns/charts/coredns
  ref:
    # Held back: CoreDNS 1.14.3+ prefetch poisons autopath answers (coredns/coredns#8390).
    # Chart 1.47.0 ships CoreDNS 1.14.6; 1.46.2 ships 1.13.1, the newest unaffected build.
    tag: 1.46.2
