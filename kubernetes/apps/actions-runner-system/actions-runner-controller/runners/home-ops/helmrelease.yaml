---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app home-ops-runner
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: *app
  values:
    # The scale set name workflows target with `runs-on:` defaults to the release name.
    githubConfigUrl: https://github.com/s0undy/home-ops
    githubConfigSecret: home-ops-runner-secret
    minRunners: 0
    maxRunners: 2
    containerMode:
      type: kubernetes
      kubernetesModeWorkVolumeClaim:
        # RWX/cephfs rather than RWO/ceph-rbd: in kubernetes mode the runner pod and
        # every workflow job pod mount this same volume, and nothing guarantees the
        # job lands on the runner's node. RWO ceph-rbd would fail to multi-attach.
        accessModes: ["ReadWriteMany"]
        storageClassName: cephfs
        resources:
          requests:
            storage: 10Gi
    controllerServiceAccount:
      name: actions-runner-controller
      namespace: actions-runner-system
    template:
      metadata:
        labels:
          runner: *app
      spec:
        affinity:
          podAntiAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 100
                podAffinityTerm:
                  labelSelector:
                    matchLabels:
                      runner: *app
                  topologyKey: kubernetes.io/hostname
        securityContext:
          fsGroup: 1001
          fsGroupChangePolicy: OnRootMismatch
        containers:
          - name: runner
            image: ghcr.io/home-operations/actions-runner:2.337.0@sha256:8b8e8326b5a08e658a0a41cfd2867afab69ca4194a152f986aa6fcd5430d09e7
            command: ["/home/runner/run.sh"]
            env:
              # Without this, kubernetes mode refuses any job that does not declare
              # its own `container:` — which is every workflow in this repo.
              - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
                value: "false"
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                memory: 4Gi
