---
apiVersion: v1alpha1
kind: SecurityProfileConfig
# Runs CRI containerd, the kubelet and all pods in a dedicated PID/mount
# namespace anchored by the sandboxd service. This is the Talos v1.14 default;
# stated explicitly because it is a deliberate choice rather than an inherited one.
workloadIsolation: true
