apiVersion: v1
kind: Service
metadata:
  name: {{ include "show.fullname" . }}
  labels:
    {{- include "show.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "show.selectorLabels" . | nindent 4 }}
